apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rabbit-app.fullname" . }}-test-connection"
  labels:
    {{- include "rabbit-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rabbit-app.fullname" . }}:{{ .Values.wordpress.service.port }}']
  restartPolicy: Never
